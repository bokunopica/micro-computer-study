STACK SEGMENT STACK
    DB 256 DUP(?)
STACK ENDS
DATA SEGMENT
    VAR1 DB 'HELLO WORLD',0DH,0AH,'$'
DATA ENDS
CODESEG SEGMENT
ASSUME CS:CODESEG,DS:DATA
START:
    MOV AX, DATA
    MOV DS, AX
    MOV ES, AX

    MOV DX, OFFSET VAR1
    MOV AH, 9
    INT 21H

    MOV AH, 4CH
    INT 21H
CODESEG ENDS
    END START
    
